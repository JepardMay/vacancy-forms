import { LoaderWrapper, LoaderElement } from '../styles';

const Loader = () => {
  return (
    <LoaderWrapper>
      <LoaderElement>
      </LoaderElement>
    </LoaderWrapper>
  );
};


export default Loader;
